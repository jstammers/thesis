 % !TEX root
%% For normal draft builds (figs not displayed hence fast compile)
%\documentclass[hyperpdf,nobind,draft,oneside]{hepthesis}
%\documentclass[hyperpdf,nobind,draft,twoside]{hepthesis}

%% For short draft builds (breaks citations by necessity)
%\documentclass[hyperpdf,nobind,draft,hidefrontback]{hepthesis}

%% For Cambridge soft-bound version
\documentclass[hyperpdf,bindnopdf,booktabs]{hepthesis}
%% For Cambridge hard-bound version (must be one-sided)
%\documentclass[hyperpdf,oneside]{hepthesis}

%% Load special font packages here if you wish

\include{preamble}
%\includeonly{interferometry}
\makeatother
%\tikzexternalize
\begin{document}
\begin{frontmatter}
    \include{frontmatter}
\end{frontmatter}

\begin{mainmatter}
    \include{introduction}
    \include{theory}
    \include{compcontrol}
   %\include{expsetup}
    \include{mots}
    \include{atompreparation}
    \include{interferometry}
    \include{conclusion}
    \printbibliography
\end{mainmatter}

\begin{appendices}
    \include{appendices}
\end{appendices}

\begin{backmatter}
    \include{backmatter}
\end{backmatter}

\end{document}

